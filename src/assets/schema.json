{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "projectrc",
  "description": "Project configuration file for luxass.dev. See more here https://projectrc.luxass.dev",
  "version": 1.0,
  "type": "object",
  "properties": {
    "readme": {
      "oneOf": [
        {
          "type": "boolean",
          "description": "Should repository readme be visible on luxass.dev/projects/<project-name>?",
          "default": false
        },
        {
          "type": "string",
          "description": "Should repository readme be visible on luxass.dev/projects/<project-name>?",
          "default": "README.md"
        }
      ],
      "default": false
    },
    "npm": {
      "description": "Does the repo have a npm package and should it be visible on luxass.dev?",
      "oneOf": [
        {
          "type": "boolean",
          "description": "Will find the first package.json in the repo and use the `name` as the npm package name",
          "default": false
        },
        {
          "type": "string",
          "description": "Will use the given package.json as the npm package name",
          "default": "package.json"
        }
      ],
      "default": false
    },
    "ignore": {
      "type": "boolean",
      "description": "Should the repo be ignored?",
      "default": false
    }
  },
  "additionalProperties": true
}
